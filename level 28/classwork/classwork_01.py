# ახსენით რისთვის გამოიყენება upper(), lower(), capitalize(), find()

# upper() გამოიყენება იმისათვის რომ ყველა ასო რაც string-შია დიდ ასოდ აქციოს

# lower() გამოიყენება იმისათვის რომ ყველა ასო რაც string-შია პატარა ასოდ აქციოს

# capitalize() აქცევს პირველ ასოს დიდ ასოს ხოლო დანარჩენს პატარად

# find() -ით ვიპოვით რამე ასოს ჩვენს string-ში ვთქვათ string-ში არის aleqsandre და უნდა ვიპოვოთ a ამ შემთხვევაში მხოლოდ იმამს გამოიტანს
#რომელიც პირველად შექხვდება