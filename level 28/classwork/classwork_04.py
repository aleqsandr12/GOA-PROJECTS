#ახსენით რა განსხავებაა მეთოდსა და ჩაშენებული-ფუნქციის შორის

# ჩაშენებული ფუნქცია (Built-in function) არის პითონში არსებული სტანდარტული ფუნქცია, მაგალითად print(), len(), type() და სხვ.
# მათთვის არ გვჭირდება რაიმე ობიექტის შექმნა, ისინი დამოუკიდებლად მუშაობენ.

# მეთოდი (method) კი არის ფუნქცია, რომელიც დაკავშირებულია კოდთან მაგ. (სტრინგთან, სიასთან, ლექსიკონთან და ა.შ.)
# მეთოდი ყოველთვის იძახება კოდის სახელით და წერტილით, მაგალითად: string.upper(), list.append()
